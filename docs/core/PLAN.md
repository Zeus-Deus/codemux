# Codemux Plan

- Purpose: Canonical roadmap and build order.
- Audience: Anyone deciding what to build next.
- Authority: Build-order document, not release-readiness proof.
- Update when: Major milestones, sequencing, or focus areas change.
- Read next: `docs/core/STATUS.md`, `docs/plans/openflow.md`, `docs/plans/windows-support.md`

## Planning Rules

- This file is about ordering and priorities, not current truth.
- Use `docs/core/STATUS.md` for repo reality and manual validation status.
- Keep subsystem execution details in `docs/plans/*`, not here.

## Roadmap At A Glance

1. Foundations landed in meaningful form: phases 0 through 9.6 established the workspace shell, multi-session terminals, pane management, notifications, browser prototype, CLI and socket control, indexing, and project memory.
2. Steps 6 through 13 (cross-cutting agent-chat work) landed on `feature/agent-chat` and merged to `main` behind the Step 13 Beta Features toggle. The full chat stack — Claude / Codex / OpenCode providers, attachments, slash commands, skills sync, MCP host runtime — ships in every release since `v0.2.0` but is OFF by default. Users opt in via Settings → Beta Features.
3. Current focus: phases 10 through 15 (OpenFlow hardening + Linux/Windows release polish), plus moving the agent-chat surface from opt-in Beta to default-on.
4. Parallel track: Phase 18 (Windows support) is **shipping** in every release. Foundation merged in `cc9b946` and shipped in `v0.1.20`/`v0.1.21`; the hardening pass (PowerShell as default shell, portable-pty fork, scrollback flush backstop, editor detection rewrite, window controls, system-process port filtering, agent-browser argv + Chromium auto-detect, control-pipe retry, Tier-3 SendInput) is on `main` and shipping. OpenFlow-on-Windows and Authenticode signing remain gated before a polished Windows v1.
5. Later: Phase 17 (macOS) has not been started.

## Ordered Phases

1. Phase 0: architecture baseline and MVP framing
2. Phase 1: stabilize prototype startup, errors, theming, and repo hygiene
3. Phase 2: define the real backend app domain model
4. Phase 3: deliver multi-terminal session management
5. Phase 4: build the workspace shell and sidebar
6. Phase 5: deliver splits and pane management
7. Phase 6: add notifications and attention workflows
8. Phase 7: ship a usable browser pane prototype toward MVP
9. Phase 8: expose browser automation for agents and tools
10. Phase 9: harden CLI and socket automation
11. Phase 9.5: add local-first codebase indexing
12. Phase 9.6: add portable project memory and handoff support
13. Phase 10: define OpenFlow core design and boundaries
14. Phase 11: build the OpenFlow runtime scaffold
15. Phase 12: integrate OpenFlow with Codemux workspace surfaces
16. Phase 13: harden the autonomous loop and intervention flow
17. Phase 14: improve quality, observability, and safety
18. Phase 15: complete Linux polish and release readiness
19. Phase 16: prepare cross-platform abstractions
20. Phase 17: add macOS support
21. Phase 18: add Windows support

## Immediate Priority Order

1. Harden OpenFlow reliability and intervention flow (the largest remaining stability gap)
2. Re-enable OpenFlow on Windows by rewriting the bash wrapper scripts in `openflow::prompts`
3. Move the agent-chat surface from Beta-gated to default-on once dogfooding settles
4. Repair the Changes-panel regression from the refined-minimal UI pass (`92965c9`). Backends, Tauri commands, wrappers, and settings rows are all intact; only the UI callers were dropped. Restore, in rough value order: the AI merge resolver entry point (`ai-merge-store.ts` has zero importers), per-file Ours/Theirs (`resolveConflictOurs`/`Theirs`), "Merge [base] into current" (`mergeBranch`), "Merge into [base]" (`mergeIntoBase`), the Recent Commits section (`gitLogEntries`), the "Against base" compare section, and Alt+Click inline diff. See `docs/features/changes-panel.md` § "Removed by the slim-panel pass" and `docs/features/merge-resolver.md`. Public docs for these were removed from the website in the meantime; restore them from git history when the buttons come back.
5. Add memory drawer UI
6. Add context menus on pane headers (workspace rows, section groups, tabs, and the changes/ports sidebar panels already have them)
7. Windows Authenticode code signing — pick OV vs EV certificate once SmartScreen friction starts showing up in user reports
8. macOS support (Phase 17)

## Cross-Cutting Steps (Tracked Outside Phase Numbering)

The Phase numbering above (1-18) is the original product roadmap. Step-numbered work below is feature-scoped initiatives that span phases and have their own per-stage plan docs under `docs/plans/`. They land in parallel with the phase work.

- **Step 6–7 — Agent-chat pane + provider scaffolding (LANDED).** AgentProvider trait + Codex/Claude adapters + Tauri command surface + chat pane UI with streaming + approvals + mode pills + permission-mode restart. Behavior doc: `docs/features/agent-chat.md`.
- **Step 8 — Attachments + context system (LANDED).** Files, folders, GitHub issues + PRs, images via `+` button + `@` mentions, paste, drop. Plan: `docs/plans/step-8-attachments.md`.
- **Step 9 — Cross-provider MCP runtime (LANDED).** Plan: `docs/plans/step-9-mcp-servers.md` + `docs/plans/step-9-codex-mcp-spike.md`. Codex MCP support deferred to Step 11.
- **Step 10 — Skills sync (LANDED; server-side since PR #112).** Originally shipped end-to-end-encrypted (XChaCha20-Poly1305, client-held machine-bound key); PR #112 replaced that with server-side plaintext storage (same model as `user_settings` sync) so GitHub OAuth / SSO users sync with no password prompt. Plan + per-stage history (E2E era): `docs/plans/step-10-skills-sync.md`. Feature (current behavior): `docs/features/skills-sync.md`.
- **Step 10.5 — Project-scoped skills sync (PLANNED, ~3-5 days).** Sync skills tied to specific git repos in addition to the user-global ones already shipping. Schema is additive: Stage 1's `user_skills` table already reserves `scope` as plaintext; 10.5 adds `project_remote_url_hash TEXT NULL` (HMAC of normalized git remote URL — server sees "skills for repo-with-this-hash" but never which repo). Trickiest piece is URL canonicalization (`https://github.com/u/r` ↔ `git@github.com:u/r.git` ↔ `https://github.com/u/r.git` are the same repo).
- **Step 11 — Codex MCP support via HTTP gateway (PLANNED).** Spike: `docs/plans/step-9-codex-mcp-spike.md`. Codex doesn't expose an MCP host API, but streamable HTTP MCP transport landed stable in Codex (Sept 2025) + `config/mcpServer/reload` lets us hot-reload without bouncing the session. Plan: ship an HTTP MCP gateway in Codemux that Codex consumes as a single MCP entry, reusing the Step 9 registry. ~40-50% of Step 9 Stage 3's complexity.
- **Step 12 — Multi-provider chat (Claude + Codex + OpenCode) (LANDED).** Feature doc: `docs/features/multi-provider-chat.md`. Research: `docs/plans/step-12-opencode-research.md`. Plan + per-stage history: `docs/plans/step-12-opencode-implementation-plan.md`. Operator UI smoke: `docs/plans/step-12-ui-smoke-checklist.md`. Stages 1-9 shipped (Stage 5 explicitly deferred to v2). Deferred follow-ups: multi-instance per provider, picker keyboard shortcuts (`Ctrl+1..9` collides with workspace switching), OpenFlow capabilities convergence.
- **Step 13 — Agent Chat Beta toggle (LANDED).** Single Settings toggle "Beta Features" that controls every Step 6–12 GUI surface so the merge to `main` ships with the new behaviour OFF by default and the legacy behaviour preserved. Unifies `enable_agent_chat` and `enable_lazy_workspace_creation` flags. Research: `docs/plans/step-13-beta-toggle-research.md`. Smoke checklist: `docs/plans/step-13-ui-smoke-checklist.md`. Settings UI in `src/components/settings/beta-features-section.tsx`. Plain-quit on toggle off (no auto-restart) keeps user data intact.
- **MCP-on-remote — Steps 2 + 3 + 4 + 7 + 8 (LANDED, `v0.6.2`).** `codemux-remote serve` headless daemon + `codemux-remote mcp` stdio bridge + 11-tool headless catalog (12 since the `v0.7.5` `worktree_create` addition) + bearer-token auth + systemd user unit + auto-register of MCP in agent configs on every serve start + background `hosts_upgrade` poller. The desktop's "Push workspace to host" flow is now zero-touch — the user clicks once and an agent on the host has a working MCP control plane. Self-contained `src-tauri/src/remote/` module ships without first extracting `codemux_core` from the Tauri crate. Plan: `docs/plans/mcp-on-remote.md`. Feature: `docs/features/remote-hosts.md`.
- **MCP-on-remote — Steps 1 + 5 + 6 + 9 (DEFERRED).** Extracting `codemux_core` out of the Tauri crate (Step 1), the desktop pull-workspace UI (Step 5), the desktop `--host` CLI flag (Step 6), and migrating the desktop's existing control endpoint from Unix-socket JSON-line to HTTP+manifest (Step 9). The wire protocol the pull flow needs is already up + tested on the daemon side; the missing piece is desktop UI + an SSH-tunnel wrapper around an already-working HTTP client. Tracked in `docs/plans/mcp-on-remote.md` "Explicitly deferred to follow-up PRs."

## Recently Completed

- **Unreleased on `main` after `v0.14.0`.** A small Agent Chat reliability + polish batch: stale-Claude-session self-heal so a dead `sdk_session_id` no longer wedges the thread (PR #177), Full-access silent-downgrade heal so a raced `bypassPermissions` boot doesn't start gating tools (PR #178), New Chat moved to the top of the session-history dropdown (PR #181), the composer's top separator line removed (PR #180), and the tooltip open/close animation removed (PR #179). Docs: `docs/features/agent-chat.md`.
- **Shipped in `v0.14.0` (2026-07-17).** Web remote access (PR #163) + its account/relay/hosted follow-on stack: account-mode Stage A (PR #166), browser-WASM iroh transport + `app.codemux.org` hosted bootstrap (PR #167), GitHub OAuth sign-in for the hosted web client (PR #168), plus `codemux remote pair` + a Tailscale-only bind scope — all default-off; the hosted `app.codemux.org` static client + `api.codemux.org` device registry are gated human deploys (not yet live). Also in the release: workspace archive (PR #175), first-send instant feedback + image-attachment perf & thumbnail fixes (PR #174), provider-native slash commands in the composer (PR #172), and two Agent Chat refinements — AskUserQuestion answers render as reply bubbles (PR #165) and a live run stops being mislabeled "interrupted" after a workspace switch (PR #164). Docs: `docs/features/web-remote-access.md`, `docs/features/workspace-archive.md`, `docs/features/agent-chat.md`, `docs/plans/web-remote-account-mode.md`, `docs/plans/app-codemux-org-hosting.md`.
- **Shipped in `v0.13.2` — perf/reliability batch** (documented in `docs/core/STATUS.md`): agent-browser session reaping during app lifetime (PR #145, issue #126), pane-tree structural sharing + `React.memo` (PR #150, issue #127), Linux WebKitGTK transcript scroll perf (PR #156, issue #129), dead-run detection & interrupted turns (PR #160, issue #154), "Send now" queued-message steering (PR #162), git-clone stall detection replacing the absolute timeout (PR #161), an SSH-bootstrap truncated-binary guard (PR #157), subagent/working-indicator settle fixes (PRs #158/#159, issue #155), and idle-scrollback serialize off the workspace-switch path (PR #152).
- **Shipped in `v0.13.1` — non-git projects + chat polish.** First-class non-git projects with an opt-in bare-`git init` "Initialize Git" affordance (PRs #147/#148; `docs/features/workspace-creation.md` § "Non-Git Projects"), chat image attachments rendered inline in sent bubbles with a click-to-open lightbox (PR #151; `docs/features/agent-chat.md` § "Attachments"), deferred-worktree auto-naming + launch-in-worktree + Context Row issue-chip restore (PRs #146/#148), and transcript smooth-scroll fixes (PRs #148/#149).
- **Shipped in `v0.13.0` — Agent Chat polish batch.** Six merges, all Agent Chat Beta surfaces. (0) **Context Row** (PR #144) — the bottom `WorkspaceContextBar` hides itself while an Agent Chat pane is the active pane in GUI chrome (`useAgentChatPaneActive()`), and its git/PR detail relocates below the composer as a read-only Context Row built on the new `WorkspaceStatusCluster`; the background-browser pill is extracted to a shared `BackgroundBrowserIndicator` so both surfaces render it identically, and `PR_CHIP_TONE` moves to `pr-status-icon.tsx` as its single home; `docs/features/agent-chat.md` § "Context Row (running-thread status)" + `docs/features/workspace-context-bar.md`. (1) **Docked live subagent activity bar** (PR #143) — a per-thread `SubagentActivityBar` between transcript and composer rolls up every running subagent across all `subagent_run` cards (`runningSubagentEntries`), jumps to a card on click, flashes green on `>0 → 0`, and unmounts when idle; it **removed** the pane-header and in-titlebar "N subagents running" pills; `docs/features/agent-chat.md` § "Docked live activity bar". (2) **Thread Scope first-send controls** (PR #142) — the above-composer scope pills become one borderless `ThreadScopeRow` below the composer on both the draft surface and the pane's new-thread empty state, with deferred + auto-named worktree creation on first send; `WorktreePicker`/`DerivativeBranchPicker` deleted; `docs/features/agent-chat.md` § "Thread Scope". (3) **Scroller engine owns stick-to-bottom** (PR #141) — `MessageList`'s hand-rolled 80px pin/snap loop deleted; the shadcn engine's `following-bottom` machine is the single tail-tracking owner on an 8px threshold; `docs/features/agent-chat.md` § "Transcript scroller". (4) **Opt-in desktop-size background-browser peek** (PR #140) — synced setting `agent_chat.background_browser_desktop_viewport` (default OFF) pins the peek's CDP viewport to 1280×800 via a new `BrowserPane` `fixedViewport` prop; `docs/features/browser.md`, `docs/features/settings-sync.md`. (5) **Background browser chip releases on run finish** (PR #139) — `publish_pane_status` calls `release_detached_agent_browser` when a run settles to `Review`/`Idle`, so the `LIVE` chip stops sticking; `docs/features/browser.md` § "Run-finished release".
- **Shipped in `v0.12.0` — GUI-mode + orchestration batch.** Five merges, all Agent Chat Beta surfaces. (1) **GUI-mode chrome** (PR #135) — with the Beta flag on for a real non-OpenFlow workspace, the four stacked chrome rows collapse to one `h-10` title bar (in-titlebar workspace tabs, a cmdk `+` launcher, inline chat favorite, rehomed right-panel/Run/subagents controls); legacy chrome byte-identical with the flag off; `docs/features/gui-chrome.md`. (2) **Workflow orchestration GUI** (PR #137) — Claude-only `Workflow` tool_use tap → in-thread `WorkflowRunCard` approval/progress/summary + a conditional Orchestration right-panel tab with per-phase agent drill-in + a `/workflow` slash command; `docs/features/workflow-orchestration.md`. (3) **Follow-up message queueing** (PR #136) — sending mid-turn queues the message (backend-authoritative) and auto-dispatches FIFO on turn end; new `TurnQueued`/`QueuedTurnDispatched`/`QueuedTurnCancelled` events; `docs/features/agent-chat.md` § "Follow-up queueing". (4) **Background browser in GUI mode** (PR #138) — an agent-opened browser stays detached instead of splitting the chat, surfaced by an inline chip + context-bar indicator + a floating `BrowserPeekOverlay` with one-click promote-to-pane; `docs/features/browser.md`. (5) **Sidebar spinner persists while subagents outlive the parent turn** (PR #134) — a stateful `SubagentTracker` defers the `Review` status until the last subagent goes terminal; `docs/features/agent-chat.md` § "Sidebar status indicators". Plus follow-up top-bar spacing/feedback + tab drag-reorder passes.
- **Shipped in `v0.11.0` — Agent Chat batch.** Seven merges, all under `docs/features/agent-chat.md` unless noted. (1) **Cross-provider subagent view** (PR #125) — one canonical `SubagentUpdated`/`SubagentSnapshot` backend model + `subagent_id` demux tag emitted by all three adapters (Claude `parent_tool_use_id`, Codex child-`threadId`, OpenCode child-`sessionID`), rendered as a Subagents orchestration card + in-pane read-only drill-in; subagent events ride the parent thread so cards survive restart with zero migration. Plan + implementation record at `docs/plans/subagent-view.md`. (2) **Activity Stream** (PR #124) — `buildTranscriptSlots` folds contiguous reasoning + tool-call runs into one `activity` slot (`ActivityBlock.tsx` + `activity-steps.ts`); `ToolGroupCard` retired. (3) **Restart auto-resume + persisted per-thread picker config** (PR #130) — `ensure_live_session` rebuilds a dead provider session on the next send/respond (same `thread_id`, `resume_cursor`), and four nullable picker columns (`model`/`effort`/`context_window`/`permission_mode`, schema v8) persist per-thread config across restart. (4) **Model picker resolved version + blurb** (PR #131) — Claude rows show `Opus 4.8 with 1M context · …` (SDK description passthrough + `ALIAS_CANONICAL_IDS` synthesis in `capabilities.rs`) instead of bare aliases. (5) **Agent browser panes route to the agent's own workspace** (PR #123) — `agent_chat_start_session` overlays the chat pane's `CODEMUX_WORKSPACE_ID`/`workspace_pty_env` onto the sidecar env, with a `resolve_workspace_id_by_cwd` cwd-hint fallback; `docs/features/browser.md`. (6) **Sidebar status from chat sessions** (PR #122) — `forward_event` maps each `ProviderRuntimeEvent` to a `PaneStatus` via `set_pane_status_by_thread` so chat workspaces light the sidebar working/needs-input/review dot; `docs/features/sidebar.md`. (7) **Ctrl+V image-paste Linux fallback** (PR #121) — a `paste_clipboard_image` Rust command reads the OS clipboard server-side when WebKit2GTK strips the payload from the JS `paste` event.
- **Shipped in `v0.10.0` — chat + design-system batch.** Five merges. (1) **Agent Chat GUI redesign** on the June-2026 shadcn chat primitives (PR #119) — transcript on the shadcn MessageScroller (replacing `react-virtuoso`), Streamdown assistant markdown, a `reasoning` ChatViewItem, tool-group/diff/task cards, a 760px composer rebuilt as one anchored `+`/`@`/slash command menu, and provider-marked assistant avatars; `docs/features/agent-chat.md`. (2) **Transcript navigation trail** (PR #120) — a slim left-gutter tick rail (one per user turn) with hover preview + click-to-jump, driven off the scroller's `visibleMessageIds`; hides below 3 turns, caps at 60 ticks; `docs/features/agent-chat.md` § "Navigation trail". (3) **Workspace context bar** (PR #116) — a read-only 42px bottom status strip carrying the active workspace's branch · kind · ahead/behind · diff · PR/issue chips · device; `docs/features/workspace-context-bar.md`. (4) **Design-system tokenization + shadcn stone-base preset** (PR #115) — all ~40 components swept off hardcoded status colors onto `src/globals.css` design tokens (176 replacements), base preset switched to `b1HYEHloH` (stone), the blue sidebar accent re-routed to ember; PR #115 also removed the `codemux-ui` skill that held theming rules, so they now live in `docs/reference/DESIGN-SYSTEM.md`. (5) **Chat Agent preset pinned leftmost** (PR #117) — the native GUI-chat launcher hoisted to a fixed leftmost slot outside the drag-reorder context; `docs/features/presets.md`. Also in this window: **Agent Chat Beta feature-flag isolation** between the installed and dev builds (PR #118 — `observability.json` is now per-build so the toggle stops leaking across builds; `docs/features/observability.md`).
- **Shipped in `v0.10.0` — cloud-push UX.** Two refinements. (1) A **cloud-push migration overlay** (PR #114, issue #23) — the push / pull-back session migration terminates each pane's local PTY and respawns it on the remote (or local) daemon, several seconds of otherwise-frozen local scrollback. A new transient `Migrating` terminal lifecycle state + `emit_migrating_for_workspace` helper (`terminal/mod.rs`) is emitted for every pane between `terminate_workspace_sessions` and `spawn_missing_ptys_for_workspace` in `workspace_push_to_host` ("Switching to <host>…") and `workspace_pull_back_impl` ("Returning to this device…"); `TerminalPane.tsx` renders a distinct "Migrating workspace" overlay card via the existing imperative DOM-overlay path and auto-dismisses (opacity fade) on `Ready` or first output byte. `Migrating` collapses to `Starting` in the persisted state — no new persisted/overview variant. (2) A **"Reinstall agent on host" button** (PR #113, issue #24) — an always-visible Settings → Hosts card that force-reinstalls `codemux-remote` + restarts its pty-daemon regardless of installed version, the dev-workflow escape hatch for when a branch rebuild keeps the version string identical so the push-time `ensure_remote_binary_current` check skips the upgrade. New `hosts_reinstall_remote` Tauri command re-probes `uname` itself (no prior Test connection) and reuses the `force_reinstall_remote_binary` helper extracted from `ensure_remote_binary_current` (uname probe → `pkill` pty-daemon → scp + chmod + verify → restart `serve`). Docs: `docs/features/remote-hosts.md`, `docs/features/terminal.md`.
- **Shipped in `v0.9.5`.** **Opt-in cloud-push diagnostic tracing** (PR #110) — the verbose per-step push-to-host trace/debug logs added during PR #15 (a 4-pane push emitted dozens of `[trace:…]`/`[client_for_workspace:…]`/`[tunnel-supervisor]`/`[daemon::spawn]` lines of noise) are gated behind the `CODEMUX_TRACE_CLOUD_PUSH` env var. New `src-tauri/src/trace.rs` (`cloud_push_enabled()` `OnceLock`-cached env check + `trace_cloud_push!` macro); ~25 call sites across `terminal/daemon_backed.rs`, `ssh/registry.rs`, `ssh/tunnel_supervisor.rs`, `pty_daemon/server.rs` moved onto it. Default keeps fewer than ten actionable lines per push; set the env var to restore the full trace with no recompile. Process-wide gate works for both the desktop app and the headless `codemux-remote` daemon. Docs: `docs/features/observability.md` § "Cloud-Push Diagnostic Tracing", `docs/features/remote-hosts.md` (troubleshooting). Plus a **Ctrl+Shift+B right-panel toggle** (PR #111 — a rebindable `toggleRightPanel` action mirroring Ctrl+B, a *true* toggle matching the tab-bar button rather than the store's by-tab-identity flip, surfaced in the command palette; `docs/reference/SHORTCUTS.md`) and **server-side skills sync** (PR #112 — replaced the end-to-end-encrypted model with server-side plaintext storage so GitHub OAuth / SSO users sync with no password prompt; dropped the `EncryptionManager` / `sync-key.enc` / `src-tauri/src/encryption/` module + the OAuth sync-password setup + the reset-sync-password dialog; one-time in-place plaintext migration of existing rows via PUT to their `remote_id`; matching additive server schema + plaintext `/api/skills` routes staged + test-DB-verified but not yet deployed; `docs/features/skills-sync.md`).
- **Shipped in `v0.9.4`.** A **Superset-style collapsible icon-rail left sidebar** (PR #108 — two-state expanded ↔ 52px icon rail with aggregate agent-status dots / notification badges on collapsed project avatars + a per-project hover flyout; `docs/features/sidebar.md`), a batch of **new rebindable keyboard shortcuts** (PR #108 — Ctrl+N New agent, Ctrl+Shift+N New workspace in project, Ctrl+O Open project, Ctrl+Shift+E Split pane down, Ctrl+Shift+? Show shortcuts; `docs/reference/SHORTCUTS.md`), and a **duplicate-project-name disambiguation** fix (PR #109 — prefer a host tag, fall back to growing the path tail; `src/lib/path.ts`, `src/stores/app-store.ts`).
- **Shipped in `v0.9.3`.** A **correctness/hardening batch** (the `bugfixes-from-superset` branch, PR #107 — ~25 targeted fixes spanning panes/tabs focus + proportion handling, terminal OSC/Kitty parsing, browser CDP modifier + multibyte-log, MCP split-direction + scroll-amount, git/diff stats + branch-name `.lock`/non-ASCII, repo-scoped GitHub caches, sidebar drag-reorder, keybind parsing, notification level, pty-daemon read-timeout, opencode-sync SQL-escaping, openflow comm-log timestamps, workspaces-sync/overview edge cases — almost all making behavior match the existing feature docs, so no feature-doc changes needed), plus a **hard-timeout guard on every agent-browser external call** (issue #96 — timeout wrappers that SIGKILL a wedged `agent-browser` and its process group; `docs/features/browser.md`) and **richer new-workspace attachment chips** (image thumbnails via the Tauri asset protocol, "Pasted image" labels, restyled linked-issue chip; `docs/features/workspace-creation.md`).
- **Shipped in `v0.9.2`.** **Structured "agent launcher" presets + a New Preset button** (`docs/features/presets.md`), **model + reasoning selection before launch** graduating from plan to shipped (`docs/plans/model-selection-before-launch.md`), the **browser pane native-feel interaction pass** (issue #99 — `docs/features/browser.md`), an **enlarged new-workspace dialog**, **circular project-picker favicons**, **orphan-leftover-dir reclaim on create**, and the **zenity self-drive** half of the file-dialog fix (issue #95).
- **Shipped in `v0.9.1`.** **WebGL hardware-GL gating** (`webgl-renderer-probe.ts`) correcting the `v0.9.0` per-keystroke input-lag regression from loading the addon on software-rendered WebGL stacks (issue #98 — `docs/features/terminal.md`), plus the first half of the **silent file-dialog fix for minimal Linux setups** (issue #95 — portal preflight, cause-specific messaging, `codemux logs`/`codemux doctor`; `docs/features/observability.md`, `docs/reference/CONTROL.md`).
- **Shipped in `v0.9.0`.** A **desktop performance pass** (issues #72–#79), **opt-in agent-run checkpoints** (issue #80), and **headless-daemon worktree provisioning parity** (issue #78). Perf: xterm.js WebGL renderer with DOM fallback, loaded unconditionally here — gated on a hardware-GL probe in `v0.9.1` after an input-lag regression (#72); PTY producer back-pressure engaged on the live path — write-pump HIGH/LOW watermarks → `pause_pty_output`/`resume_pty_output`, in-process reader gains a real `flow_paused` gate, self-heal fail-safes on both spawn paths (#73); blocking `workspace.rs` (#74) and `files.rs` (#79, + quotepath-proof `git check-ignore` parsing) commands off the GTK main thread; agent-chat events per-thread over Tauri Channels via `attach_agent_chat_output` with generation-tokened detach (#75); chat transcript virtualized with `react-virtuoso` (#77). Plus: new-branch creation fetches `origin/<base>` first with offline fallback, desktop + daemon (#76); run checkpoints snapshot the working tree at chat-session start to `refs/codemux/checkpoints/<thread>` with a pane-header restore (#80, Settings → Agent toggle, design note `docs/plans/agent-run-checkpoint.md`); the daemon's `worktree_create` MCP tool provisions like the desktop — includes copy + background setup commands via the shared UI-free `scripts::run_setup_commands` core (#78), verified by a containerized clean-host e2e (`scripts/e2e/daemon-worktree-setup-e2e.sh`, PR #93). Feature docs: `docs/features/{terminal,agent-chat,workspace-creation,setup-teardown,remote-hosts}.md`.
- **Shipped in `v0.8.0`.** Three changes. (1) **OpenCode conversation sync across cloud-push (issue #16)** — the OpenCode counterpart to PR #15's Claude conversation sync, so a pushed/pulled OpenCode pane continues the same session. Because OpenCode stores all conversations in one SQLite DB that can't be rsynced without clobbering the host's other history, the sync uses OpenCode's own `export`/`import` CLI (deliberately *not* the issue's `rusqlite`-extractor plan). New `src-tauri/src/ssh/opencode_db_sync.rs` + generalized `build_agent_relaunch_command` in `terminal/daemon_backed.rs`; verified end-to-end against a Docker SSH host (`scripts/e2e/opencode-sync-e2e.sh` + env-gated `opencode_sync_roundtrip.rs`). Feature doc: `docs/features/opencode-conversation-sync.md`. (2) **Project-favicon cache-bust fix (PR #71)** — `resolveImageUrl(input, cacheBust)` appends a changing `&v=` token to derived favicon-service URLs so a re-saved project image visibly refreshes instead of serving stale WebView cache; direct/data URLs untouched. (3) **Dev-only Tauri mock runtime (PR #82)** — a dual-guarded `src/dev/` shim (`tauri-mock.ts` + `mock-fixtures.ts`) installs a faithful `window.__TAURI_INTERNALS__` so the real UI boots in a plain browser under `npm run dev` with seed data, enabling the browser-pane screenshot workflow; tree-shaken out of production and dormant under `npm run tauri:dev`. Feature doc: `docs/features/dev-mock-runtime.md`.
- **Multi-device robustness + remote-persistence pass (shipped in `v0.7.9`).** Four threads on top of repo-unit sync. (1) **SSH tunnel health in the UI** — `spawn_tunnel_status_forwarder` bridges the supervisor's `TunnelStatus` watch channel to a `tunnel-status-changed` event; a zustand `tunnel-status-store` fed by an app-root `useTunnelStatusEvents` hook drives a sidebar pill (amber "Reconnecting…", red "Connection lost — re-push"). (2) **Host persistence** — `hosts_upgrade` reads the daemon's `live_terminals` (via `codemux-remote serve status`) and DEFERS the systemd-unit restart while host agents run (`UpgradeOutcome::Skipped`); the local pty-daemon idle-reaps after 1h with zero sessions (hard re-check under lock). (3) **Workspaces-sync robustness** — project-first pull with a real protected root (local-only `default_branch` column + `resolve_default_branch`/`ensure_origin_head` + `workspaces_adopt_project` "Pull project"), per-row creation lock serializing adopts, client-side `dedupe_sibling_rows`, daemon-side `collapse_main_for_uid`/`normalize_main_workspaces`, uid-keyed collision-safe host paths, non-destructive `workspaces_reconcile_copy`. (4) **OpenFlow comm-log tee** on the daemon-backed spawn path via the shared `comm_log_entry_for_chunk` helper. Touches `src-tauri/src/{ssh/registry.rs,hosts_upgrade.rs,pty_daemon/server.rs,terminal/{mod,daemon_backed}.rs,git.rs,workspace_paths.rs,workspaces_sync.rs,commands/{workspaces_sync,hosts}.rs,remote/workspace.rs,bin/codemux_remote.rs}` + `src/{App.tsx,stores/tunnel-status-store.ts,hooks/use-tunnel-status-events.ts,components/layout/sidebar-workspace-row.tsx,components/workspaces-overview/*,tauri/{events,commands}.ts}`. Feature docs: `docs/features/{remote-hosts,persistent-agents,workspaces-sync,workspaces-overview,openflow}.md`; plan: `docs/plans/repo-unit-sync.md`.
- **Operate a remote workspace in place — "Open on host" (shipped in `v0.7.9`, issue #64).** A third remote mode alongside Push (copy up) and Pull (copy down): an overview "Open on host" action creates a local *attach-in-place* workspace whose terminal/agent runs on the host in its real directory with live streaming and **nothing copied under `~/.codemux/` locally**. `WorkspaceSnapshot` gained `remote_cwd` + `attach_only`; the daemon-backed terminal path spawns into `remote_cwd` over the existing SSH-tunneled pty-daemon. Persistence is real — `ssh::tunnel::build_remote_command` reuses a still-running daemon (pidfile liveness probe) or spawns it detached (`setsid`/`nohup`), so closing the app leaves the host process running and reopening re-tunnels + reattaches. Bundled with the `v0.7.9` project_uid path re-key (issue #65 — uid-keyed local adopt landings + JSONL-sync remote-dir resolution) and a server-side unique index preventing duplicate cross-device rows (issue #66). Feature doc: `docs/features/remote-in-place.md`.
- **Repo-unit sync (shipped in `v0.7.8`).** Cross-device sync used to clone a project's default-branch checkout into a divergent full copy under `~/.codemux/worktrees/<project>/main` — its own git object store, drifting from the real repo, and (because it lived in the worktrees tree) misclassified as a deletable disposable worktree. Now a git repo (root + worktrees) is treated as one shared-history unit. New `git_canonical_root` + `RepoRoot` + `is_divergent_copy` + `is_protected_repo_root` in `src-tauri/src/git.rs` classify a checkout divergence-safely via `git rev-parse` (unit-tested for main/worktree/bare/copy/protected); `WorkspaceSnapshot` gained `protected` + `divergent_copy` flags (`#[serde(default)]`, stamped at create time in `set_workspace_project_root` plus a background boot backfill `backfill_workspace_protection` wired in `lib.rs`, mirrored onto the TS type). Overview UI: a protected root shows a `repo root` badge and offers only "Close workspace…"/detach (never "Delete worktree…"), a legacy divergent copy shows an amber `standalone copy` warning. Adoption routes a `main`-kind row through `create_synced_root_shell` landing at `~/.codemux/projects/<repo>`; `workspace_push_to_host` pushes a repo root to `~/.codemux/projects/<repo>` on the host (`conventional_remote_root_path`) and `workspace_pull_back_impl` is root-aware with a legacy `worktrees/` fallback. The three flags are snapshot-local only (NOT added to `workspaces_sync`/cloud — sibling rows have no delete action). Cross-device SSH round-trip + one-click reconcile of existing copies are explicit follow-ups. Touches `src-tauri/src/{git.rs,workspace_paths.rs,state/state_impl.rs,lib.rs,commands/workspaces_sync.rs,commands/hosts.rs}` + `src/components/workspaces-overview/workspace-overview-row.tsx` + `src/tauri/types.ts`. Plan: `docs/plans/repo-unit-sync.md`. Feature docs: `docs/features/workspaces-sync.md`, `docs/features/workspaces-overview.md`.
- **Docker container ports + new-workspace dialog polish (shipped in `v0.7.8`).** `scan_ports` now folds in `detect_docker_ports`: it shells out to `docker ps` (cached availability via `DOCKER_CLI_STATE` so a missing binary doesn't spawn every cycle) and surfaces each published TCP host port under a dedicated **Docker** sidebar group labeled by container name — recovering ports the Linux `/proc` scan can't attribute because `docker-proxy` runs as root. Scoped to open codemux worktrees by matching the compose `working_dir` label against `all_workspace_paths`; kill action hidden for these rows. Plus two new-workspace-dialog fixes: issue-link dedup surfaces a toast + an inline "already has a workspace / Open it" notice instead of silently activating an existing workspace and dropping the typed message; the "Link pull request" button now reuses the polished `PrPickerPanel` (search, `PrStatusIcon` state-colored icons, skeleton, "Link ↵") matching the issue picker. Touches `src-tauri/src/{ports.rs,state/state_impl.rs}`, `src/components/layout/sidebar-ports-popover.tsx`, `src/components/overlays/new-workspace-dialog.tsx`, `src/components/github/pr-picker.tsx`. Feature docs: `docs/features/ports.md`, `docs/features/workspace-creation.md`.
- **Cross-device pull/adopt/close hardening (shipped in `v0.7.7`).** Two audits (a deep code audit + a live SSH round-trip against a loopback host) of the remote-host push/pull/adopt + inventory-poll pipeline surfaced a cluster of failure-mode/lifecycle bugs, all off the happy path. Fixes: `workspaces_adopt_synced` checks `pull_outcome.ok` and reverts to a re-pullable sibling on failure (`unlink_workspace_sync_from_local`) instead of leaving a broken linked shell; both close paths tear down the SSH tunnel supervisor + cached client (`forget_workspace_client`/`shutdown_supervisor`) so a pushed workspace stops leaking the tunnel + remote daemon on close; all three adopt paths stamp `project_uid` (`set_workspace_project_identity`) so adopted workspaces converge with siblings instead of wiping the daemon-derived uid; the inventory poller undeletes a remote-discovered tombstone on reappear + dedupes duplicate ids in one envelope (no more cloud-row churn); `pull_workspace_back` classifies a missing remote dir as `RemoteNotFound` via a `CMX_REMOTE_DIR_OK/MISSING` stdout sentinel (was a fragile `exit status: 7` match); `provision_workspace_mcp_config` resolves the remote `$HOME` → absolute `.mcp.json` command path (the systemd `%h` specifier broke remote MCP auto-discovery); shell-injection hardening on `ssh_upload_executable`/`ssh_write_file` via tilde-aware `shell_escape`; tunnel-socket hash widened 12→16 hex chars; a stale-link pull guard (`detect_same_branch_project_conflict` intersects against live workspace ids so a closed-but-stale sync row stops phantom-blocking Pull); and a sidebar fix so the workspace-row X button no longer overlays/swallows clicks on the linked-issue badge. New env-gated (`CODEMUX_E2E_SSH_HOST`) integration harness `src-tauri/tests/codemux_ssh_roundtrip.rs` + regression unit tests. Touches `src-tauri/src/{commands/workspace.rs,commands/workspaces_sync.rs,database.rs,hosts_inventory.rs,workspaces_sync.rs,state/state_impl.rs,ssh/{push,bootstrap,registry}.rs}` + `src/components/layout/sidebar-workspace-row.tsx`. Feature docs: `docs/features/workspaces-sync.md`, `docs/features/remote-hosts.md`.
- **Agent-created host-workspace pull/adoption fix + terminal hardening (`v0.7.5`).** A workspace an agent built directly on a `codemux-remote` host (e.g. `git worktree add` + `workspace_create`) previously pulled to the desktop as an empty worktree — the pull rsynced from the conventional `~/.codemux/worktrees/<project>/<branch>` path instead of the workspace's real on-host `path`. Fixes: a new local-only `origin_path` column on `workspaces_sync` (additive ALTER) carries the daemon's real path so `workspace_pull_back_impl` rsyncs from the right place; worktree-kind adoption delegates to `adopt_worktree_via_repo_rsync` (whole-repo rsync + `git worktree prune`/`add`) to rebuild the cross-machine-broken `.git` gitfile locally and works for local-only repos; `upsert_workspace_sync_from_server` uses `COALESCE(server, local)` for `project_uid`/`workspace_kind` so a null cloud value never clobbers a locally-derived identity. The same work added a **`worktree_create` tool to the headless daemon catalog (11 → 12 tools)** + `src-tauri/src/remote/git.rs`. Terminal side: **PTY output flow-control / backpressure** plumbing (daemon `SetFlowPaused` + HIGH/LOW watermarks — inert on the live render path at the time; engaged post-`v0.8.0` by issue #73), split panes inherit the workspace cwd, the workspace-switch freeze fix (throttled xterm write pump), and removal of the redundant standalone "Diff Viewer" new-tab (+) / command-palette entry points. Plan: `docs/plans/remote-workspace-pull-fix.md`. Feature docs: `docs/features/workspaces-sync.md`, `docs/features/remote-hosts.md`, `docs/features/terminal.md`, `docs/features/diff-viewer.md`.
- **First-class project identity + `main`/`worktree` kind (shipped in `v0.7.4` — Superset-adapted).** Replaced the implicit, path-derived "project" concept with a stable deterministic identity: `project_uid = UUIDv5(canonical git remote ?? absolute project_root)`, computed in a new `src-tauri/src/project_identity.rs` (also `derive_kind` for `main` vs `worktree` from `.git` dir-vs-file). The key deviation from Superset is determinism — every checkout of the same remote converges on the same uid with zero coordination, so no replicated projects table or reconcile step is needed. Phase 1 (daemon registry columns + idempotent ALTER migration + create-time stamp + boot sweep, host-inventory poller threading, local `workspaces_sync` columns, overview grouping + kind badge, conflict guard) and Phase 2 (desktop `WorkspaceSnapshot.project_uid`/`workspace_kind` stamped in `set_workspace_project_root`; cloud `codemux_workspaces` round-trip deployed + verified on the VPS — 303 server tests, 44 prod rows untouched) are both complete. Phase 3 (a first-class `projects` table for project-level metadata) is optional/future. Touches `src-tauri/src/{project_identity.rs,remote/workspace.rs,bin/codemux_remote.rs,hosts_inventory.rs,database.rs,workspaces_sync.rs,commands/workspaces_sync.rs}` + `src/tauri/commands.ts` + `src/components/workspaces-overview/*`. Plan: `docs/plans/project-identity.md`. Feature docs: `docs/features/workspaces-sync.md`, `docs/features/workspaces-overview.md`.
- **Asymmetric workspace auto-publish from `codemux-remote` hosts (`v0.7.x`).** A workspace an agent creates directly on a host (via the MCP `workspace_create` tool or a manual `codemux-remote workspace register`) now surfaces in every dev device's Workspaces overview without an explicit push from the laptop. New `codemux-remote workspace list` CLI subcommand reads the daemon's SQLite directly (no running daemon required); new `hosts_inventory.rs` background poller runs every ~60 s on each dev device, SSHes the CLI, and reconciles into `workspaces_sync` as sibling-only rows keyed by `(host_server_id, origin_uid)`; the existing 30 s push tick uploads them to the cloud, so other devices see new host-side workspaces within ~90 s. Schema is additive (`ALTER TABLE workspaces_sync ADD COLUMN origin_uid TEXT`). Pull-conflict guard via `AdoptionPreview.same_branch_project_exists_at` + `SameBranchProjectBlock` dialog variant refuses to clobber a local workspace already on the same branch of the same project. The asymmetry is deliberate — dev devices keep manual push/pull (user agency over what leaves the device), hosts auto-publish (always-on, exist to be used as hosts). Touches: `src-tauri/src/bin/codemux_remote.rs` (CLI), `src-tauri/src/hosts_inventory.rs` (poller), `src-tauri/src/database.rs` (additive column + 5 helpers), `src-tauri/src/commands/workspaces_sync.rs` (conflict detection), `src/components/workspaces-overview/pull-to-device-dialog.tsx` (`SameBranchProjectBlock` variant). 33 new unit tests + 3 integration tests. Plus three UX fixes: configured-host buckets stay visible in the overview even when empty (a device the user just added shows up immediately, not only after the first workspace lands on it); close-workspace paths reconcile + push the sync row immediately so the overview never briefly mis-tags a just-closed workspace as "lives on another device"; `ssh::probe` falls back to `~/.local/bin/codemux-remote` when the PATH lookup fails. Feature docs: `docs/features/workspaces-sync.md` § "Asymmetric publish model", `docs/features/remote-hosts.md` § "Background host-inventory poller".
- **MCP-on-remote (`v0.6.2`).** `codemux-remote` graduated from a slim PTY-daemon binary into a full headless Codemux daemon: axum HTTP server on loopback with bearer-token auth + manifest at `<state-dir>/manifest.json`, self-contained SQLite workspace registry, an 11-tool stdio MCP catalog (`codemux-remote mcp`; `worktree_create` added in `v0.7.5` brings it to 12), and Unix-only `serve` / `serve status` / `serve stop` / `mcp` / `workspace register` CLI subcommands. Push auto-provisioning makes the user-facing flow one click: `provision_serve` installs + starts the systemd user unit, `push_workspace` drops a workspace-scoped `.mcp.json`, `register_workspace_on_remote` inserts the pushed workspace into the daemon's registry, and `mcp_register.rs` idempotently adds a `codemux` MCP entry to user-level agent configs (`~/.claude.json`, `~/.codex/config.toml`, `~/.cursor/mcp.json`). A new background `hosts_upgrade` poller silently re-bootstraps every registered host whose `codemux-remote` version differs from the bundled binary (`hosts_upgrade.rs`). Binary upload uses a `ssh-cat` pipeline to dodge an OpenSSH 9+ tilde-expansion bug. 26 unit tests + 8 end-to-end integration tests in `src-tauri/tests/codemux_remote_serve_mcp.rs`. Feature doc: `docs/features/remote-hosts.md`; plan with the deferred Steps 1/5/6/9: `docs/plans/mcp-on-remote.md`.
- **Workspaces overview + cross-device workspaces sync (`v0.6.1`).** Account-wide full-screen overview opened from the sidebar (`Workspaces` button under `Automations`), backed by a new `/api/workspaces` endpoint set + local `workspaces_sync` mirror on the same 30s pull/push cadence as `hosts_sync` / `automations_sync`. Phase 1–5 shipped end to end: device-grouped layout + filters (Phase 1–2), sibling-device adoption — host-backed and clone-from-git fallback — (Phase 3), confirm-before-push + 10-second undo + cross-device git-HEAD divergence chip + elapsed-time pill (Phase 4a–4d), first-run welcome banner + "how it works" popover (Phase 5). Touch points: `src-tauri/src/workspaces_sync.rs`, `src-tauri/src/commands/workspaces_sync.rs`, `src/components/workspaces-overview/*`. Feature docs: `docs/features/workspaces-overview.md`, `docs/features/workspaces-sync.md`.
- **Seven new agent presets (`v0.6.0`).** Antigravity, Copilot, Cursor Agent, Amp, Grok, Droid, Mastracode added to the builtin preset set — each launches in its CLI's skip-permissions / YOLO mode. Droid gets the same temp-file injection treatment as Gemini because interactive `droid` has no `--skip-permissions` flag (`inject_agent_context` writes `{"sessionDefaultSettings":{"autonomyLevel":"high"}}` and launches `droid --settings <path>`). All seven verified in a clean Docker container.
- **Multi-editor launcher + family grouping.** The IDE detection list went from 8 to 19 editors (added Windsurf, Trae, Fleet, Lapce, PyCharm, PhpStorm, RubyMine, CLion, Rider, DataGrip, Android Studio). Title-bar launcher and workspace context-menu submenu partition entries into labelled sections (VS Code family / Modern editors / JetBrains / Other), preserving canonical order within each group and only rendering headers when more than one family is detected. Windsurf / Trae / Lapce got Windows `%LOCALAPPDATA%` fallback paths. Feature doc: `docs/features/ide-integration.md`.
- **PR-state sidebar icon swap.** When a worktree workspace has a PR, the sidebar row's leading icon turns into the PR-state-colored icon (open=green, merged=purple, closed=red, draft=gray) and the trailing duplicate PR pill is removed. Primary / remote / OpenFlow rows keep their existing glyphs.
- **Automations (`v0.5.2`).** Scheduled agent runs — a named prompt + agent + RFC 5545 recurrence that fires on a user-chosen host, creates a fresh git worktree per fire, runs the agent headlessly, and records a real `succeeded` / `failed` / `skipped_*` terminal status. Phase 1 shipped the local data model, recurrence engine, scheduler, executor, command surface, and a first-class left-sidebar Automations view. Phase 2 added account sync (`automations_sync` against the live `/api/automations` endpoints), host routing, a `codemux-remote scheduler` systemd service provisioned at host bootstrap, a stuck-run reconciler, and the Phase F GitHub-backbone repo transport (a remote host clones/fetches the project's git remote). Eight `automation_*` MCP tools. Feature doc: `docs/features/automations.md`; plans: `docs/plans/automations.md`, `docs/plans/automations-sync.md`; research: `docs/research/superset-automations.md`.
- **Refined-minimal UI pass.** Sidebar redesigned (refined-minimal aesthetic), slim Changes panel + ADE-native right sidebar, settings panel aligned with shared primitives, title-bar menu consolidated into the sidebar footer, preset-bar icons + tab-bar drop indicator aligned, agent-chat composer polished (Local pill dropped, dev spawn button removed, sidebar "New agent" locked to home).
- **Default-branch detection (`v0.5.0`).** Sidebar branch pill is seeded from `origin/HEAD` and follows live remote-branch changes; derivative-branch picker drops the phantom `origin/<name>` rows so a user can't pick a remote-only ref by accident.
- **Per-worktree notification mute.** Right-click → "Mute notifications" on a sidebar workspace row toggles `notifications_muted`; muted state shows a bell-off icon and skips agent-completion notifications.
- **Title-bar resource monitor.** CPU/memory monitor in the title bar with accurate per-process counting (no longer counts threads as processes).
- **Hooks: agent status across Codex / Gemini / OpenCode / Pi.** Status indicators (red/amber/green dots) now light up for every supported provider, not just Claude. Claude idle reminder is filtered out so it no longer raises a phantom stuck-red dot.
- **SSH workspace push with Claude conversation sync (`v0.4.0`–`v0.5.0`).** The "push workspace to host" action that was deferred in step 2d has landed: rsync the worktree, spawn the remote daemon, attach the local UI through the SSH-forwarded socket, and sync the Claude conversation across local/remote ends. New `ssh::push`, `ssh::registry`, `ssh::tunnel_supervisor` modules.
- **Persistent PTY daemon promoted to default.** Every shell now spawns inside `codemux pty-daemon`; the setting was removed (only the `CODEMUX_DISABLE_PTY_DAEMON=1` panic-button env var remains). Agents survive app close, the supervisor adopts the running daemon on relaunch, and the 3-failures-in-60s crash circuit breaker disables the daemon path safely on regression.
- **MCP server Phase 1 / 1.5 / 1.6 (vexis-agent integration).** Added `terminal_write`, `terminal_read`, `workspace_open`, `app_status`, `port_list` (Phase 1); `worktree_create`, `preset_apply`, `preset_list` (Phase 1.5); `workspace_close`, `pane_close`, `issue_list`, `issue_get`, `issue_link_workspace` (Phase 1.6). Tool count went 31 → 44.
- **Browser stream stability fix (LANDED).** Unified port keying around `workspace_id`, PID-tracked daemons (was port-tracked), atomic teardown, symmetric `TcpListener::bind` bind probe, reactive `stream_url` reconnect on the frontend. Eliminates the silent stream failure that appeared after multiple concurrent worktrees used the browser. Plan archived at `docs/archive/browser-stream-fix.md`.
- **Browser viewport presets.** `codemux browser viewport <mobile|tablet|desktop|WxH|reset>` resizes the actual viewport via CDP so CSS media queries fire and screenshots capture at the simulated dimensions. MCP exposes `browser_viewport` + `browser_viewport_presets`.
- **Performance pass** (`v0.2.3`–`v0.2.4`): high-frequency app-state emits coalesced into 16ms windows, `transition-all` scoped, markdown view + workspace-tied components stop re-rendering on every backend tick, workspace-switch mount-time IPC roundtrips cut, editor file read + language module import parallelised, worktree-include listener no longer re-attaches every tick, primitive fingerprint for `ensure-draft-when-empty` effect, all git/gh shell-outs moved off the GTK main thread.
- **Review tab unfreeze** on repos with thousands of PRs (paginated fetch).
- **Per-workspace display isolation** for agent-spawned GUI apps (X11/Wayland sandbox). Opt-in for human persona, default-on for agent persona; the human-default revert restored DISPLAY inheritance for the GUI flows where users actually paste images via Ctrl+V.
- **Bitwarden-style password derivation** ported for cross-product login parity with Vexis (Argon2id + HKDF, pinned in CI).
- **Stability**: stopped leaking agent-chat sidecars on workspace/tab/pane close; throttle reattach replay so multi-MB `pending_output` can't freeze the renderer; cap `pending_output` by bytes (256 MiB) rather than chunks.
- **Windows post-release hardening** between `v0.1.21` and `v0.2.0`: forked `portable-pty` to add `STARTF_USESHOWWINDOW + SW_HIDE`, switched `default_shell()` to prefer PowerShell, taught `agent_context::inject_agent_context` to emit `$env:VAR` syntax, rewrote `find_editors()`, extracted `<WindowChrome />`, extended scrollback flush timeout to 10s + added `flush_cache_to_disk` backstop, Windows system-process port filter, sonner toast for preset failures, `\r` preset terminators, Tier-3 SendInput input injection, Claude Code hooks on Windows, agent-browser argv fix + ERROR_PIPE_BUSY retry, Edge auto-detect, `resolve_binary` Windows discovery branch.
- **PTY child process tree cleanup** on session close: single `killpg(pid, SIGKILL)` via a central `terminate_pty_session` helper. Closes the TOCTOU window the old SIGTERM→200ms→SIGKILL dance exposed to PID recycling.
- Session persistence: terminal scrollback save/restore and adapter-based resume
- GitHub issue integration (link to workspaces, picker, auto-branch naming)
- Browser wait conditions, JS evaluation, CSS style inspection (MCP tools)
- Custom keybind editor in Settings
- Auto-update system (AppImage / NSIS in-app update, toast notification)
- Built-in file editor with CodeMirror, syntax highlighting, markdown preview
- AI merge conflict resolver with temp-branch safety model
- MCP server for agent self-orchestration (52 tools via JSON-RPC 2.0, expanded through Phase 1 / 1.5 / 1.6 vexis-agent integration steps and the eight `automation_*` tools)
- Settings panel (keyboard shortcuts, appearance, project scripts, beta features, sync, skills, MCP, permissions)
- Auth system (GitHub OAuth, email/password, email verification, encrypted token storage)
- Synced settings (per-user server-synced with offline cache)
- Claude CLI adapter for OpenFlow
