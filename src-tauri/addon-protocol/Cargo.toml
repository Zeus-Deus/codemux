[package]
name = "codemux-addon-protocol"
version = "1.0.0"
edition = "2021"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
semver = "1"
schemars = "0.8"
url = "2"
