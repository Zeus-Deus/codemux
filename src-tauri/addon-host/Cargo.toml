[package]
name = "codemux-addon-host"
version = "1.0.0"
edition = "2021"
license = "MIT"

[dependencies]
codemux-addon-protocol = { path = "../addon-protocol" }
rquickjs = "=0.13.0"
serde_json = "1"
serde = { version = "1", features = ["derive"] }
